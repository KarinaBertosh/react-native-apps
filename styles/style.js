import { StyleSheet } from "react-native";

export const gStyles = StyleSheet.create({
    main: {
        flex: 1,
        padding: 20,
        paddingTop: 60,
    },
    title: {
        textAlign: 'center',
        fontSize: 20,
        color: '#333',
        fontFamily: 'mt-light',
    }
});